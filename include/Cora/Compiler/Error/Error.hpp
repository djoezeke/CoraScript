#ifndef CORA_COMPILER_ERROR_ERROR_H
#define CORA_COMPILER_ERROR_ERROR_H

namespace cora::compiler
{
    namespace error
    {
        class Error
        {
        private:
            /* data */
        public:
            Error();
            ~Error();
        };

    } // namespace error

} // namespace cora::compiler

#endif // CORA_COMPILER_ERROR_ERROR_H
