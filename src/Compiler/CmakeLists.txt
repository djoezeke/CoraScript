# ===================================================================
# Copyright © 2025 Sackey Ezekiel Etrue
# Distributed under the terms of the MIT license.
# ===================================================================

#--------------------------------------------------------------------
# Basic Configs   
#--------------------------------------------------------------------

set(COMPILER_SOURCES 

    "${COMPILER_DIR}/Basic/SourceManager.cpp"
    "${COMPILER_DIR}/Basic/SourceLocation.cpp"

    "${COMPILER_DIR}/AST/Nodes.cpp"
    "${COMPILER_DIR}/AST/Statements.cpp"
    "${COMPILER_DIR}/AST/Expressions.cpp"

    "${COMPILER_DIR}/Error/Error.cpp"
    
    "${COMPILER_DIR}/Builtin/Builtin.cpp"
    "${COMPILER_DIR}/Builtin/Class.cpp"
    "${COMPILER_DIR}/Builtin/Module.cpp"

    "${COMPILER_DIR}/Parser/Token.cpp"
    "${COMPILER_DIR}/Parser/Lexer.cpp"
    "${COMPILER_DIR}/Parser/Parser.cpp"

    "${COMPILER_DIR}/Runtime/Scope.cpp"
    "${COMPILER_DIR}/Runtime/Value.cpp"
    "${COMPILER_DIR}/Runtime/Variable.cpp"
    "${COMPILER_DIR}/Runtime/Interpreter.cpp"
)

#--------------------------------------------------------------------
# Build Compiler Library.
#--------------------------------------------------------------------

add_library(Compiler STATIC ${COMPILER_SOURCES})

add_library(Cora::Compiler ALIAS Compiler)

target_include_directories(Compiler
	PUBLIC
        ${PROJECT_SOURCE_DIR}/modules
		${CMAKE_CURRENT_SOURCE_DIR}
        ${PROJECT_SOURCE_DIR}/include
)

# ===================================================================
