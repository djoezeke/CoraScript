# ===================================================================
# Copyright © 2025 Sackey Ezekiel Etrue
# Distributed under the terms of the MIT license.
# ===================================================================

#--------------------------------------------------------------------
# Basic Configs   
#--------------------------------------------------------------------

set(CORA_PARSER_SOURCES 

    "${CORA_PARSER_DIR}/Token.cpp"
    "${CORA_PARSER_DIR}/Lexer.cpp"
    "${CORA_PARSER_DIR}/Parser.cpp"
)

#--------------------------------------------------------------------
# Build Parser Library.
#--------------------------------------------------------------------

add_library(Parser STATIC ${CORA_PARSER_SOURCES})

add_library(Cora::Parser ALIAS Parser)

target_include_directories(Parser
	PUBLIC
        ${PROJECT_SOURCE_DIR}/include
		${CORA_PARSER_DIR}
)

target_link_libraries(AST
	PRIVATE
		Cora::Basic
)

# ===================================================================
